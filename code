#1. Сначала вводим слово
word_to_check = input("Введите слово: ")


#2. Вводим переменные length и middle
length = len(word_to_check)  #длина слова
middle = length//2           #индекс середины слова


#3. Проверяем четность/нечетность в количестве букв в слове
if length % 2 == 0:         #если число букв в слове четное, то выводятся 2 средние буквы
   middle = len(word_to_check)//2    #ищется середина слова
   print (word_to_check[middle-1:middle+1])   #выводится диапозон: середина-1 и середина+1
else:
   print (word_to_check[middle])     #в обратном случае выводится одна средняя буква
